import { formatNumber } from '../utils/format.js';
import { getCurrentLevel } from '../state/pokerLogic.js';
export default function PositionBoard({ tournamentState }){ const bb=getCurrentLevel(tournamentState)?.bb||1; return <section className='position-board panel'><div className='row position-board-head'><div className='round-label'>Runde: <span>{tournamentState.roundNumber}</span></div></div><div className='positions-grid'>{tournamentState.players.map(p=><div key={p.id} className={`position-card ${p.eliminated?'eliminated':''} ${p.currentPosition==='Dealer'?'dealer-highlight':''}`}><div className='pos-name'>{p.eliminated?'Slått ut':p.currentPosition}</div><div className='position-player-name'>{p.name}</div><div className='small-note'>Stack: {formatNumber(p.chips)}</div><div className='small-note'>{p.chips>0?Math.floor(p.chips/bb):0} BB</div></div>)}</div></section>;}
