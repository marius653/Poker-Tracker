import { useEffect, useState } from 'react';
export default function StackModal({ isOpen, players, onClose, onSave }){ const [stackValues,setStackValues]=useState([]); useEffect(()=>{if(isOpen)setStackValues(players.map(p=>p.chips));},[isOpen,players]); if(!isOpen)return null; return <div className='modal-backdrop active' onMouseDown={e=>{if(e.target===e.currentTarget)onClose();}}><div className='modal'><div className='utility-line'><div><h3>Endre stacks</h3><p className='small-note modal-note'>Sett stack til 0 for å slå ut spiller.</p></div><button type='button' className='btn btn-gray btn-small' onClick={onClose}>Lukk</button></div><div className='edit-grid'>{players.map((p,i)=><div className='edit-card' key={p.id}><div className='edit-card-name'>{p.name}</div><div className='small-note modal-note'>{p.eliminated?'Slått ut':p.currentPosition}</div><input type='number' min='0' step='10' value={stackValues[i]??0} onChange={e=>setStackValues(v=>v.map((x,idx)=>idx===i?e.target.value:x))}/></div>)}</div><div className='footer-actions align-end'><button type='button' className='btn btn-primary' onClick={()=>onSave(stackValues)}>Lagre</button></div></div></div>; }
